import React from "react";
import ReactDOM from "react-dom/client";
import AppWrapper from "./components/App/App.jsx"; // НЕ App!

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<AppWrapper />);
